## cXObjectImpl::InventoryCommandParam_GetGuid(InventoryCommandParam &) Analysis
Address: 0x801069B4
Size: 52 bytes (from DVD build)
Release version (cm3-build25) address: 0x801b24a0
Pseudocode source: src/asm_decomp/cXObjectImpl.cpp (lines 26085-26123)

### Function Signature
```cpp
long long InventoryCommandParam_GetGuid(InventoryCommandParam &param);
```

### Parameter Handling
- r3: this pointer (not used)
- r4: InventoryCommandParam & (reference to param structure)

### Stack Frame
- stwu 1, -16(1)
- Save LR at offset 20(1)
- Local variables: offsets 8, 10, 12 used for work area

### Assembly Flow
1. **Load and test parameter byte at offset 4**
   - lbz 0, 4(4) (load unsigned byte at param[4])
   - srwi. 9, 0, 7 (test bit 7 of the byte)
   - bne .L_80106A0C (branch if bit 7 is set — error path)
   - [Bit 7 set indicates error/invalid state]

2. **Valid state path: extract GUID from param**
   - lhz 0, 2(4) (load unsigned halfword at param[2])
   - li 9, -1 (set r9 = -1 for default)
   - lhz 11, 0(4) (load unsigned halfword at param[0])
   - li 6, 0 (clear r6)
   - sth 9, 12(1) (store -1 to stack[12])
   - extsh 5, 0 (sign-extend param[2] to r5)
   - clrlwi 4, 11, 24 (clear upper 24 bits of param[0] → r4)
   - stb 11, 8(1) (store low byte to stack[8])
   - sth 0, 10(1) (store param[2] to stack[10])
   - li 7, 0 (clear r7)
   - addi 8, 1, 12 (pass stack location)
   - .long 0x480040ad (call cXObjectImpl::InterpValue)

3. **Check InterpValue result**
   - cmpwi 3, -1 (check if error in r3)
   - bne .L_80106A14 (branch if r3 is not -1)
   - cmpwi 4, -1 (check if error in r4)
   - bne .L_80106A14 (branch if r4 is not -1)

4. **Error path (bit 7 set OR InterpValue failed)**
   - li 3, 0 (return r3 = 0)
   - b .L_80106A18 (jump to epilogue)

5. **Success path (loaded interpreted GUID)**
   - lha 3, 12(1) (load signed halfword from stack[12])
   - [This is the result from InterpValue]

6. **Return**
   - lwz 0, 20(1) (restore LR)
   - mtlr 0
   - addi 1, 1, 16 (clean up stack)
   - blr (return)

### Struct Layout (inferred from operations)
```
InventoryCommandParam {
  unsigned short param0;      // offset 0
  unsigned short param2;      // offset 2
  unsigned char flags;        // offset 4 (bit 7 = error/validity flag)
  unsigned char param5;       // offset 5
  unsigned short param6;      // offset 6
  // ... additional fields
};
```

### Return Value
**Return as int (r3 only)**
- Success: r3 = interpreted GUID value (from InterpValue)
- Error: r3 = 0 (param flags invalid OR InterpValue failed)

### Data Flow
1. **Input:** InventoryCommandParam with GUID fields at offsets 0, 2
2. **Validity check:** Bit 7 of flags[4] must be clear
3. **Interpretation:** Call InterpValue with extracted fields
4. **Output:** Return interpreted GUID or 0 on error

### Key Insight
This function is NOT a simple getter. It:
1. Checks if the parameter is in a valid state (bit 7 of flags)
2. Extracts GUID components from multiple fields (param0, param2)
3. Runs them through an interpretation system (InterpValue)
4. Returns the interpreted GUID or 0 on error

The InterpValue call suggests GUID values may be expressions or conditionals
that need runtime evaluation (not just static values).

### Call Pattern
```cpp
lhz(param[0]) → r4 (clear upper 24 bits)
extsh(param[2]) → r5 (sign-extend)
li(r6=0, r7=0, r9=-1)
stb(param[0]>>8, stack[8])
sth(param[2], stack[10])
sth(-1, stack[12])
InterpValue(r3=this, r4, r5, r6, r7, stack[12])
```

### Related Functions
- cXObjectImpl::InterpValue() — expression/condition evaluator
- Common helper across all behavior tree parameter extraction

### Comparison with GetCount
Similar function exists for GetCount (0x80106A28) with nearly identical pattern,
suggesting both are interpreter-based parameter getters.

### C++ Pattern Hints
- Complex parameter interpretation system
- Expression evaluation at runtime
- Validity check before processing (flags byte)
- Stack-based work area for InterpValue results
