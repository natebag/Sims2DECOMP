## ObjFnTableQuickData::~ObjFnTableQuickData(void) Analysis
Address: 0x8010FF70
Size: 92 bytes
Pseudocode source: src/asm_decomp/ObjFnTableQuickData.cpp (lines 60-91)

### Stack Frame
- Save r30, r31
- Parameter: r3 = this pointer, r4 = destruction flags (likely)

### Operations (in order)
1. **Save parameters**
   - mr 31, 3 (save this to r31)
   - mr 30, 4 (save flag parameter to r30)

2. **Update vtable (first transition)**
   - lis 9, -32698
   - addi 9, 9, 8216
   - Resulting address: 0x801fbab8 (same as ctor vtable)
   - stw 9, 0(31) (update m_vtable)

3. **Delete m_fnData if non-null**
   - lwz 3, 16(31) (load m_fnDataArray into r3 for delete)
   - .long 0x481c1289 (call __builtin_delete(void *))

   **Note:** The disassembly shows offset 16 being deleted, which matches m_fnDataArray
   This suggests the function deletes the array allocated in m_fnDataArray

4. **Update vtable (second transition)**
   - lis 9, -32698
   - addi 9, 9, 8376
   - Resulting address: 0x801fbc38
   - stw 9, 0(31) (update m_vtable again)

5. **Conditional self-deletion**
   - andi. 0, 30, 1 (test bit 0 of destruction flag)
   - beq .L_8010FFB8 (branch if not set — skip delete)
   - mr 3, 31 (load 'this' for delete)
   - .long 0x481c126d (call __builtin_delete(void *))

6. **Return**
   - Restore r30, r31, return

### Struct Layout (from operations)
```
ObjFnTableQuickData {
  void *m_vtable;           // offset 0 (updated twice)
  ObjectDataID m_dataID;    // offset 4-11
  void *m_fnData;           // offset 12
  void *m_fnDataArray;      // offset 16 (deleted in step 3)
  int m_refCount;           // offset 20
};
```

### Vtable Transitions
The destructor updates the vtable twice:
- First update: to 0x801fbab8 (same as ctor — normal state)
- Second update: to 0x801fbc38 (alternate vtable — possible destruction-in-progress marker)

This suggests a "complete object destructor" pattern where the vtable is updated
to mark the object as being destroyed.

### C++ Pattern
The destructor:
1. Updates vtable to a state indicating deletion is in progress
2. Deletes the allocated m_fnDataArray (if it exists)
3. Potentially updates vtable again
4. Conditionally deletes 'this' based on a flag passed in r4

The conditional self-deletion pattern suggests this is a dual-purpose destructor:
- If flag & 1 == 0: only clean up members (m_fnDataArray), don't delete self
- If flag & 1 == 1: clean up members AND delete self

This is common in object systems where the deletion responsibility may be elsewhere
(e.g., smart pointers, parent containers).

### Key Details
- r31 holds 'this' throughout
- r30 holds the destruction flag
- Two vtable updates suggest a state machine or GC-style marking
- Address 0x801fbc38 (second vtable) is different from ctor address by 0x0180
- Both vtable addresses are in read-only code/data section (8xxxx xxxx)
