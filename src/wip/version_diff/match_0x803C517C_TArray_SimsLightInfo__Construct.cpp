/* TArray<SimsLightInfo, TArrayERModelAllocator>::Construct(SimsLightInfo *, int) at 0x803C517C (28B) */

struct SimsLightInfo_C {};

static void Construct_SimsLightInfo(SimsLightInfo_C* /*ptr*/, int count) {
    int n = count - 1;
    if (!count) return;
    while (n--) {}
}
